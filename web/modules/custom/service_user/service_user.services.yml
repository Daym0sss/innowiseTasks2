services:
  service_user.user_service:
    class: Drupal\service_user\services\UserServiceObject
  cache.backend.null:
    class: Drupal\Core\Cache\NullBackendFactory

parameters:
  http.response.debug_cacheability_headers: true
  twig.config:
    debug: true
    aotu_reload: true
    cache: false